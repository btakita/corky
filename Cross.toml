[target.aarch64-unknown-linux-gnu]
image = "ghcr.io/cross-rs/aarch64-unknown-linux-gnu:edge"
pre-build = [
  "dpkg --add-architecture arm64",
  "apt-get update -y",
  "apt-get install -y libssl-dev libssl-dev:arm64 pkg-config"
]
